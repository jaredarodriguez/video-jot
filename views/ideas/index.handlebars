{{#each ideas}}
    <div class="card card-body">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <a class="btn btn-dark" href="/ideas/edit/{{id}}">Edit</a>
    </div>

{{else}}
    <p>No videa ideas listed</p>
{{/each}}